# @agentspec/coverage

Deterministic behavioural coverage analysis for Agent Lint specs.

## Metrics

- route coverage
- handoff coverage
- tool coverage
- constraint coverage
- fallback coverage
- test scenario coverage

The package reports percentage coverage, uncovered branches and recommended test scenarios. It uses declared tests and named scenarios. No LLM calls are made.
